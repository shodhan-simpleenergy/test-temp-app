apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: simple-energy-ingress
  namespace: test-temp
spec:
  ingressClassName: nginx   # Modern way instead of deprecated annotation
  rules:
    - host: simpleenergy.local
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: simple-energy-service   # Your service name
                port:
                  number: 5000              # Must match service targetPort


